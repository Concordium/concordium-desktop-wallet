import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Switch from '../Switch';

import { argTypes, Template } from './deps';

<Meta title="Cross App Components/Switch" component={Switch} argTypes={argTypes} />

# Switch

The `Switch` component allows the same props as a regular `<input type="checkbox" />`, with the addition of `error`.

<Canvas>
    <Story name="Switch" args={{
        children: ['Enable setting'],
        checked: false,
    }}>
        {Template.bind({})}
    </Story>
</Canvas>

The following props are the most important to work with `Switch`:

<ArgsTable story="Switch" />

