import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import TextArea from '../TextArea';

import { argTypes, Template, decorators } from './deps';

<Meta title="Components/Form/Fields/TextArea" component={TextArea} argTypes={argTypes} decorators={decorators} />

# TextArea

The TextArea component allows the same props as a regular `<textarea />`, with the addition of `error`.

<Canvas>
    <Story name="Primary" args={{
        placeholder: 'Name',
        name: 'name'
    }}>
        {Template.bind({})}
    </Story>
</Canvas>

The following are the most important props to work with `TextArea`:

<ArgsTable story="Primary" />

## With label

<Canvas>
    <Story name="With Label" args={{
        placeholder: 'Name',
        name: 'name',
        label: 'Name'
    }}>
        {Template.bind({})}
    </Story>
</Canvas>

## Without auto-scale

<Canvas>
    <Story name="No Auto Scale" args={{
        placeholder: 'Name',
        name: 'name',
        label: 'Name',
        autoScale: false
    }}>
        {Template.bind({})}
    </Story>
</Canvas>